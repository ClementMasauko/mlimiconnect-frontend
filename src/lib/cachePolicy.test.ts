import{describe,expect,it}from"vitest";import{mayCacheRequest,mayCacheResponse}from"./cachePolicy";describe("authenticated cache isolation",()=>{it.each(["/api/orders","/app/dashboard","/admin/users","/messages"])("never caches private path %s",path=>expect(mayCacheRequest({url:`https://mlimiconnect.example${path}`})).toBe(false));it("never caches authorization or cookie requests",()=>{expect(mayCacheRequest({url:"/logo.png",headers:{Authorization:"Bearer secret"}})).toBe(false);expect(mayCacheRequest({url:"/logo.png",headers:{Cookie:"sessionid=secret"}})).toBe(false);});it("never caches private or cookie responses",()=>{expect(mayCacheResponse({ok:true,headers:{"Cache-Control":"private"}})).toBe(false);expect(mayCacheResponse({ok:true,headers:{"Set-Cookie":"sessionid=secret"}})).toBe(false);});it("allows public revisioned assets",()=>expect(mayCacheRequest({url:"/assets/app-abc.js"})).toBe(true));});
