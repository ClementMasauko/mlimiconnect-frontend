import{describe,expect,it}from"vitest";import{appLocale,formatCurrency,formatDate,formatQuantity}from"./locale";describe("localized agricultural values",()=>{it("selects Malawi locales",()=>{expect(appLocale("ny")).toBe("ny-MW");expect(appLocale("en")).toBe("en-MW")});it("formats without hard-coded separators",()=>{expect(formatCurrency(2500,"en")).toContain("2,500");expect(formatQuantity(50,"kg","ny")).toContain("kg");expect(formatDate("2026-08-29","ny")).toBeTruthy()})});
