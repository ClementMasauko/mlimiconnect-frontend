import { useEffect,useState } from "react"; import api from "../../lib/api"; import Card from "../../components/ui/Card";
type Provider={name:string;configured:boolean}; export default function ProviderReadiness(){const[rows,setRows]=useState<Provider[]>([]);useEffect(()=>{api.get<Provider[]>("/api/admin/providers/").then(r=>setRows(r.data));},[]);return <div className="mx-auto max-w-5xl p-4 py-8"><h1 className="text-3xl font-bold">Provider readiness</h1><p className="mt-2 text-slate-500">This page reports configuration presence only; secrets are never returned.</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{rows.map(row=><Card key={row.name} className="p-6"><strong className="capitalize">{row.name.replaceAll("_"," ")}</strong><p className={`mt-2 font-semibold ${row.configured?"text-green-700":"text-amber-700"}`}>{row.configured?"Configured":"Not configured"}</p></Card>)}</div></div>}
