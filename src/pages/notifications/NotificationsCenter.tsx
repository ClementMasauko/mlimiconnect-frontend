import { useState } from "react";
import { Bell, CheckCircle2, CloudRain, MessageSquare, Settings, AlertTriangle } from "lucide-react";
import { Link } from "react-router-dom";
import Card from "../../components/ui/Card";
const initial = [{ id: 1, type: "Orders", title: "Order confirmed", message: "50kg maize sold to Shoprite Lilongwe — MWK 185,000", time: "2 hours ago", read: false, icon: CheckCircle2, color: "text-green-700 bg-green-50" }, { id: 2, type: "Farm alerts", title: "Heavy rain alert", message: "Heavy rainfall expected tomorrow. Protect harvested crops.", time: "3 hours ago", read: false, icon: CloudRain, color: "text-blue-700 bg-blue-50" }, { id: 3, type: "Messages", title: "New message", message: "A buyer asked about your tomato delivery timeline.", time: "Yesterday", read: true, icon: MessageSquare, color: "text-indigo-700 bg-indigo-50" }, { id: 4, type: "Farm alerts", title: "Pest detection result", message: "Fall armyworm detected in maize field — treatment advice is available.", time: "Yesterday", read: true, icon: AlertTriangle, color: "text-amber-700 bg-amber-50" }];
export default function NotificationsCenter() { const [notifications, setNotifications] = useState(initial); const unread = notifications.filter(n => !n.read).length; const markAll = () => setNotifications(current => current.map(n => ({ ...n, read: true }))); return <div className="mx-auto max-w-5xl"><div className="mb-7 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold uppercase tracking-[.15em] text-green-700">My account</p><h1 className="mt-1 text-3xl font-extrabold tracking-tight dark:text-white">Notifications</h1><p className="mt-2 text-slate-500">{unread ? `${unread} unread updates need your attention.` : "You're all caught up."}</p></div><div className="flex gap-2"><button onClick={markAll} className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-semibold hover:bg-slate-50 dark:border-gray-700 dark:bg-gray-900">Mark all read</button><Link to="/app/profile/notifications-management" className="rounded-lg border border-slate-300 bg-white p-2 text-slate-600 hover:bg-slate-50 dark:border-gray-700 dark:bg-gray-900" aria-label="Notification preferences"><Settings size={19} /></Link></div></div><Card className="overflow-hidden p-0"><div className="border-b border-slate-200 px-5 py-4 text-sm font-bold dark:border-gray-800">All updates</div><div className="divide-y divide-slate-100 dark:divide-gray-800">{notifications.map(n => <button key={n.id} onClick={() => setNotifications(current => current.map(item => item.id === n.id ? { ...item, read: true } : item))} className={`flex w-full gap-4 px-5 py-5 text-left transition hover:bg-slate-50 dark:hover:bg-gray-800 ${n.read ? "" : "bg-green-50/60 dark:bg-green-950/10"}`}><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-full ${n.color}`}><n.icon size={19} /></span><span className="min-w-0 flex-1"><span className="flex items-start justify-between gap-4"><strong className="text-slate-900 dark:text-white">{n.title}</strong><small className="shrink-0 text-slate-500">{n.time}</small></span><span className="mt-1 block text-sm text-slate-600 dark:text-gray-300">{n.message}</span><span className="mt-2 block text-xs font-semibold text-green-700">{n.type}</span></span>{!n.read && <span className="mt-2 h-2 w-2 rounded-full bg-green-600" />}</button>)}</div></Card></div>; }
