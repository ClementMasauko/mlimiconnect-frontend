import { Link, useLocation } from "react-router-dom";
import { Bell, ChevronRight, MapPin, Settings, User } from "lucide-react";

const links = [{ to: "/app/profile", label: "Personal information", icon: User }, { to: "/app/profile/address-book", label: "Addresses & contacts", icon: MapPin }, { to: "/app/profile/notifications-management", label: "Notifications", icon: Bell }, { to: "/app/profile/settings", label: "Account preferences", icon: Settings }];
export default function AccountNav() { const { pathname } = useLocation(); return <nav className="space-y-1">{links.map(({ to, label, icon: Icon }) => <Link key={to} to={to} className={`flex items-center gap-3 rounded-lg px-3 py-3 text-sm font-semibold ${pathname === to ? "bg-green-50 text-green-800 dark:bg-green-950/30 dark:text-green-400" : "text-slate-600 hover:bg-slate-100 dark:text-gray-300 dark:hover:bg-gray-800"}`}><Icon size={18} /><span className="flex-1">{label}</span><ChevronRight size={16} className="opacity-50" /></Link>)}</nav>; }
