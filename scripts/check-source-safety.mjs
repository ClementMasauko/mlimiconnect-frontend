import{readFileSync,readdirSync}from"node:fs";import{extname,join}from"node:path";const files=[];const walk=dir=>readdirSync(dir,{withFileTypes:true}).forEach(item=>item.isDirectory()?walk(join(dir,item.name)):files.push(join(dir,item.name)));walk("src");const failures=[];for(const file of files.filter(file=>[".ts",".tsx"].includes(extname(file)))){const source=readFileSync(file,"utf8");if(/\balert\s*\(/.test(source))failures.push(`${file}: native alert()`);if(/:\s*any\b|\bas any\b|<any>/.test(source))failures.push(`${file}: explicit any`);}if(failures.length){console.error(failures.join("\n"));process.exit(1);}console.log("Source safety passed: no native alert() or explicit any usage.");
